# MongoDB Configuration
MONGO_DB_URL=mongodb://localhost:27017/student_db # Corrected connection URL without the database name inline
MONGO_DB_NAME=student_db                 # Separate line for the database name

# Application Configuration
SECRET_KEY=your_secret_key_here
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=debug
PORT=8000

# JWT (JSON Web Token) Configuration (if applicable)
JWT_SECRET_KEY=your_jwt_secret_key_here
JWT_ALGORITHM=HS256

# CORS (Cross-Origin Resource Sharing) Settings (if needed)
ALLOWED_ORIGINS=http://localhost,http://127.0.0.1
